# AIR 项目审阅与改进报告

日期：2026-08-20

## 1. 项目理解

AIR 是一条基于 LangGraph 的多智能体综述论文生产流水线。核心链路是：多源文献检索与筛选 → PDF 摄入和 RAG → 引用预验证 → 大纲 → 初稿 → 引用守门与真实性核查 → 同行审稿 → 多轮修订 → 格式检查、图表和 LaTeX。项目当前最重要的设计原则是“只允许引用可信清单中的正式发表文献”，并通过程序化验证为 LLM 的生成行为加硬门禁。

本次重点检查了 `README.md`、`src/graph/pipeline.py`、`src/graph/state.py`、Writer/Reviewer/Citation Guard、格式与图表链路以及离线测试。

## 2. 审稿—修改循环难以提质的根因

原循环虽然能限制轮数并回退到历史最高分稿，但缺少跨轮稳定参照：

1. Reviewer 每轮独立重评，没有上一轮十维分数作为锚点，容易更换关注维度。
2. 旧问题没有稳定 ID 和状态；Reviewer 漏掉旧问题时，系统无法区分“已解决”与“本轮没检查”。
3. Writer 收到较长的自由文本审稿意见，修改范围过大，容易整篇重写，修好一个维度同时损坏另一个维度。
4. 停滞检测和历史最优稿只看单一总分，无法识别引用硬伤、Critical 问题减少等实质变化。
5. Reviewer 未直接获得程序化字数、章节、引用与格式事实，主观判断可能与系统真实状态冲突。
6. 配置默认验收阈值原为 75/100，而 README 和 Reviewer 决策规则均为 40/50，存在门槛不一致。

## 3. 已完成的改进

### 3.1 固定量表与复审锚点

- 新增固定十维评分解析，只有十项完整时才按逐项求和；不再用“缺项补 3 分”的方式虚构分数，解析不完整时回退 Reviewer 明示的总分。
- 从第二轮开始向 Reviewer 提供上一轮逐维评分，并要求无实质证据变化时保持分数；通常单轮最多变化 1 分。
- 给 Reviewer 注入程序化质量摘要：正文字数、可信文献数、实际引用数、必填章节、引用守门修复数、NOT_FOUND 数和格式检查结果。

### 3.2 跨轮问题台账

- 主要问题使用稳定的 `R-*` ID，保存状态、优先级、问题描述和验收证据。
- Reviewer 必须逐条更新旧问题为“已解决 / 部分解决 / 未解决”；如果 Reviewer 漏写旧的开放问题，程序会保留它，避免评价焦点漂移。
- 新问题必须能定位到章节、引用编号或客观事实，减少同义重复登记。

### 3.3 有限修订契约

- 每轮从开放问题中按 Critical/高/中/低排序，只选最多 4 项作为本轮修订契约。
- 每项都带验收标准；Writer 被要求只做契约内的局部修改，并保持未涉及章节、关键论点和有效引用不变。
- 原始审稿意见仍作为辅助背景，但上下文预算从 20000 字符降到 8000 字符，且不得据此自行扩张修改范围。

### 3.4 更可靠的收敛与回退

- 新增质量向量：`引用硬门禁是否通过 → 50 分制总分 → Critical 问题数 → 开放问题数 → 硬伤总数`。
- 停滞判断改为比较相邻轮次质量向量；同分但 Critical/开放问题减少也算实质进步。
- 历史最优稿也按质量向量选择，避免高分但仍带引用硬伤的稿件胜出。
- 默认验收阈值统一为 `80/100 = 40/50`。

### 3.5 其他修复

- `finalize` 会按实际成图补齐缺失的正文图占位符，并按 taxonomy/timeline/trend/comparison 类型放入较合适的章节。
- 修复 Reviewer 流式输出把纯字符串写入 LangGraph `messages` 的问题，现保留真实消息对象，改善 checkpoint/resume 和用量元数据兼容性。
- 补充缺失的运行依赖 `matplotlib` 和推荐的 `langchain-chroma` 独立集成，避免图表模块无法导入以及继续依赖已弃用的 community Chroma 路径。
- 引用验证测试已更新为 mock 统一 HTTP 重试入口，与当前网络层实现一致。
- README 已同步本轮机制和问题状态。

## 4. 验证结果

- `python -m compileall -q src tests`：通过。
- `.venv\Scripts\python.exe -m pytest -q`：**112 passed**。
- 仅剩一条第三方警告：ChromaDB 在 Python 3.14 下调用了计划于 Python 3.16 移除的 asyncio API，不影响当前测试。生产环境建议优先使用 Python 3.11 或 3.12。
- 本次未运行付费 LLM/API 的完整论文生成，以避免未经确认的模型费用。离线逻辑、解析、收敛、修订契约和图表同步均已有测试覆盖。

## 5. 仍存在的主要问题

1. **引用—正文内容级一致性仍未根治。** Citation Guard 能检测作者名错配，但无法可靠判断一句技术结论是否真的由对应论文支持。仅做词面相似度会对中英跨语言和领域通用词产生较多误报。
2. **MECE、章节重复与术语一致性仍主要依赖模型。** 当前复审协议能让问题持续被追踪，但缺少确定性的术语表、章节职责和重复度指标。
3. **审稿推荐文献缺少持久拒绝缓存。** 已拒绝的预印本或无关建议可能在后续轮次再次触发搜索。
4. **缺少可量化的端到端回归基准。** 目前能证明逻辑正确，但尚不能用数据证明“相同素材下，新循环比旧循环平均提高多少分、回退多少、花费多少”。
5. **中文元数据和本地 LaTeX 环境问题仍在。** 这两项需要新的数据源策略和部署层方案，不适合仅靠提示词解决。

## 6. 建议的下一步方向

优先级建议如下：

1. 建立 3–5 个固定主题的 `--skip-retrieval` 回归样本，记录每轮十维分数、问题关闭率、引用硬伤、字数、Token 和成本；旧循环与新循环各运行至少 3 次，比较均值和方差。
2. 增加“Claim–Evidence”审计节点：抽取带引用的原子论断，从对应论文的全文块中检索证据，由独立模型输出 `SUPPORTED / PARTIAL / UNSUPPORTED`，仅将高置信 UNSUPPORTED 送入修订契约。
3. 为每篇论文生成一次结构化证据卡（研究对象、方法、数据集、主要结论、限制），Writer 只从证据卡组织论述，进一步降低内容归属错配。
4. 增加术语表和章节职责表，并使用段落嵌入相似度检测跨章节重复；这些指标一并进入客观质量摘要。
5. 在状态中保存审稿推荐文献的“已尝试/已拒绝”集合，按 DOI/归一化标题持久去重，避免重复 API 消耗。
6. 完成上述 A/B 数据后再调整 `MAX_REVISIONS`、`STAGNATION_LIMIT` 和每轮契约上限；不要只凭单篇结果调参。

推荐下一次迭代先做第 1 项。它能验证本次改造是否真正降低评分方差并提高问题关闭率，也会为后续 Claim–Evidence 节点提供客观基线。
